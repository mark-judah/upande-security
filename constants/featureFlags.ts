// dev1 build: Visitor Badge feature is active here (unlike the current
// kaitet-livesite hotfix on Ngetich/main, which has this set to `false`
// because production there doesn't have badges rolled out yet).
export const VISITOR_BADGE_ENABLED = true;
